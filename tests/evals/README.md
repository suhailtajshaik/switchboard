# Eval scenarios

Scripted counterpart scenarios for the call-behavior harness — format,
runner contract and the hard-check rubric are in `../../docs/evals.md`.
Run with `DRY_RUN=true` against the fake relay peer; CI gates on hard
checks only. Add a scenario whenever you fix a call-behavior bug.
