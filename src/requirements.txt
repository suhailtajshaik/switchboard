# Switchboard runtime dependencies — pinned (spec §1: Python >= 3.12).
# Mirrors scripts/setup.sh step 7; bump deliberately and re-run conformance.
apscheduler==3.11.2
claude-agent-sdk==0.2.100
fastapi==0.136.3
httpx==0.28.1
python-telegram-bot==22.8
twilio==9.10.9
uvicorn[standard]==0.49.0
websockets==16.0
